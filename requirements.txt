# ===================================================================
# Requisitos do Projeto CARINA
# Data da Revisão: 02 de Outubro de 2025
# ===================================================================

# --- Frameworks Principais de Computação e IA ---
# PyTorch com suporte explícito para CUDA 11.8
torch
torchvision
torchaudio

# --- Ferramentas de Análise de Dados ---
# Numpy é usado extensivamente para operações numéricas e de tensores.
numpy
# Pandas é usado pelo ChildhoodAnalyzer para processar dados de tráfego.
pandas
# Scikit-learn é uma dependência comum em projetos de ML e usado para calibração.
scikit-learn

# --- Ferramentas de Visualização e UI ---
# Flet é o framework para toda a interface gráfica do painel de controle.
flet>=0.20.0
# Matplotlib é usado para gerar os gráficos de análise do Captum e o mapa estático.
matplotlib
# Folium foi mantido do seu arquivo original para futuras visualizações de mapa.
folium

# --- Ferramentas de Análise e Explicação de IA (XAI) ---
# Captum é a biblioteca da Meta/Facebook usada para a análise de decisão (Integrated Gradients).
captum
# PyTorch Geometric é essencial para o funcionamento do Agente Estrategista (GAT).
torch_geometric
# TensorBoard é usado para logar métricas de treinamento dos agentes.
tensorboard

# --- Ferramentas de Sistema ---
# Psutil pode ser usado para monitorar o uso de recursos do sistema.
psutil
# Prometheus-client é usado pelo MetricsManager para expor as métricas do sistema.
prometheus-client
# Websockets é usado pelo Serviço de Dashboard (SDS) para comunicação com a UI.
websockets
# xxhash é usado para hashing rápido (visto no carina.py)
xxhash

# --- Dependência Especial do SUMO ---
# TraCI é a biblioteca oficial para a comunicação entre Python e SUMO.
traci

# --- Outras Utilitários ---
# Usado para parsing de rede
lxml
# Usado para análise de rede (map_generator)
Shapely
# Usado pelo map_generator
pyproj
# Usado pelo map_generator
Rtree